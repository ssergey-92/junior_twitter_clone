version: '3.9'
services:

  db:
    image: postgres:16
    container_name: test_postgresql
    restart: always
    stop_signal: SIGKILL
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=test_for_junior_twitter_clone
    ports:
      - '6000:5432'